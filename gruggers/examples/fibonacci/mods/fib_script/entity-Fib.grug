memo_list: List = list_number()

export fib_naive(number: number) {
    print_number(_fib_naive(number))
}

export fib_iterative(number: number) {
    result: number = 0
    if number < 0 {
        result = 0
    } else if number == 1 {
        result = 1
    } else {
        a: number = 1
        b: number = 1
        i: number = 2
        while i < number {
            temp: number = a + b
            a = b
            b = temp
            i = i + 1
        }
        result = b
    }
    print_number(result)
}

export fib_memoized(number: number) {
    _fib_memo(number)
    # print_number(_fib_memo(number))
    # print_list_number(memo_list)
}

export print_list() {
    print_list_number(memo_list)
}

local _fib_memo(x: number) number {
    if list_number_len(memo_list) > x {
        return list_number_get(memo_list, x)
    }
    if x == 0 or x == 1 {
        list_number_push(memo_list, 1)
        return 1
    }
    a: number = _fib_memo(x - 1)
    b: number = _fib_memo(x - 2)
    list_number_push(memo_list, a + b)
    return a + b
}

local _fib_naive(n: number) number {
    if n <= 1 {
        return 0
    }
    if (n == 1 or n == 2) {
        return 1
    }
    return _fib_naive(n - 1) + _fib_naive(n - 2)
}
